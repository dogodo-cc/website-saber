$color-main = blue;
* {margin: 0;padding: 0;}

body {
  color: $color-main;
  text-align: center;
  display: flex;
  flex-direction: column;
  height 100vh;
  overflow: hidden;
}

nav {
  a.active {
    background-color: blue;
    color: #fff;
  }
  height 60px;
  line-height: 60px;
}

.contain {
  flex: 1;
  }

footer {
  background-color: #ccc;
  height 60px;
  line-height: 60px;
  bottom:0;
  left 0;
  right: 0;
}
